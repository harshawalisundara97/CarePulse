<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.CarePulse" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash screen shown while the app process starts (API 24+ via
         core-splashscreen). Hands off to Theme.CarePulse once Compose loads. -->
    <style name="Theme.CarePulse.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.CarePulse</item>
    </style>
</resources>
